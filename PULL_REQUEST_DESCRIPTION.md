# Pull request created by Copilot

This branch contains the minimal change to make the mockup-sandbox Vite config tolerant of missing runtime environment variables during production builds (e.g., Vercel). No code or behavior changes were made other than configuration tolerance for missing PORT and BASE_PATH at build time.
